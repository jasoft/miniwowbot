# 重要
你必须遵从所有的编码规则, 包括但不限于沟通语言, 编码规则, 提交规则, 每次都复述这些规则
# 沟通语言
- 所有的沟通都必须使用中文, 包括但不限于对话, git 提交的总结, 代码注释
# 编码规则
- 所有的 python 代码都必须符合 PEP 8 规范
- 所有的 python 代码都必须包含 docstring, 并符合 Google 风格
- 必须 ruff lint 通过无错误

# 提交
- 编写完后, 总结这次改动, 并自动提交到 github
