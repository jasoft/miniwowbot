"""
游戏界面坐标常量定义
包含所有直接点击的固定坐标位置
分辨率: 720x1280
"""

# ============================================================
# 基础界面坐标
# ============================================================

# 设置按钮坐标
SETTINGS_BUTTON = (669, 107)

# 返回按钮坐标（左上角）
BACK_BUTTON = (712, 8)

# 地图按钮坐标
MAP_BUTTON = (350, 50)

# 账号头像坐标（用于切换账号）
ACCOUNT_AVATAR = (14, 43)

# ============================================================
# 战斗相关坐标
# ============================================================

# 技能栏坐标（5个技能位置，从左到右）
SKILL_POSITIONS = [
    (100, 560),  # 技能位置1
    (230, 560),  # 技能位置2
    (360, 560),  # 技能位置3
    (490, 560),  # 技能位置4
    (620, 560),  # 技能位置5
]

# ============================================================
# 每日领取相关坐标
# ============================================================

# 每日奖励箱子坐标（相对于"战斗"按钮向上偏移190像素）
# 实际使用: touch((战斗按钮中心x, 战斗按钮中心y - 190))
DAILY_REWARD_BOX_OFFSET_Y = -190

# 每日奖励确认按钮
DAILY_REWARD_CONFIRM = (458, 854)

# 快速挂机领取按钮
QUICK_AFK_COLLECT_BUTTON = (366, 708)

# 邮箱面板「一键领取」按钮。位置固定，用于 OCR 查找失败时的兜底点击：
# 邮箱面板存在入场渲染延迟，且历史上 OCR 曾把「一键领取」识别成「键领取」（漏字）。
MAIL_CLAIM_ALL_BUTTON = (475, 901)

# 活动面板底部「兑换」标签。位置固定，用于 OCR 查找失败时的兜底点击：
# 该标签点不到会让整个奖券兑换流程被静默跳过（券攒够了也换不到），
# 而面板渲染延迟是常见原因。
EVENT_EXCHANGE_TAB_BUTTON = (579, 1057)

# ============================================================
# 区域切换相关坐标
# ============================================================

# 关闭区域切换菜单按钮
CLOSE_ZONE_MENU = (80, 212)

# ============================================================
# 账号切换相关坐标
# ============================================================

# 账号下拉箭头
ACCOUNT_DROPDOWN_ARROW = (572, 599)

# 账号列表滑动区域
ACCOUNT_LIST_SWIPE_START = (480, 800)
ACCOUNT_LIST_SWIPE_END = (480, 700)

# 登录按钮
LOGIN_BUTTON = (356, 732)

# 派遣按钮
DEPLOY_BUTTON = (280, 1111)

# 一键领取
ONE_KEY_REWARD = (550, 969)

# 一键派遣
ONE_KEY_DEPLOY = (366, 969)

# 确认按钮
DEPLOY_CONFIRM_BUTTON = (366, 850)
